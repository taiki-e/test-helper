name: CI

permissions: {}

on:
  pull_request:
  push:
    branches:
      - main
      - dev
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

env:
  CARGO_INCREMENTAL: 0
  CARGO_NET_GIT_FETCH_WITH_CLI: true
  CARGO_NET_RETRY: 10
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUSTDOCFLAGS: -D warnings
  RUSTFLAGS: -D warnings
  RUSTUP_MAX_RETRIES: 10

defaults:
  run:
    shell: bash --noprofile --norc -CeEuxo pipefail {0}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  tidy:
    uses: taiki-e/github-actions/.github/workflows/tidy.yml@dc24bff2366e8ddeea5e0e4168df3d6f4b93f596 # 2026.8.8
    permissions:
      security-events: write # for github/codeql-action/*
    with:
      clippy-target: x86_64-unknown-linux-gnu,i686-unknown-linux-gnu,aarch64-unknown-linux-gnu,armv7-unknown-linux-gnueabihf,riscv64gc-unknown-linux-gnu,powerpc-unknown-linux-gnu,powerpc64le-unknown-linux-gnu,s390x-unknown-linux-gnu,loongarch64-unknown-linux-gnu,sparc64-unknown-linux-gnu
      clippy-rustflags: --cfg valgrind
  codegen:
    needs: tidy
    uses: taiki-e/github-actions/.github/workflows/gen.yml@dc24bff2366e8ddeea5e0e4168df3d6f4b93f596 # 2026.8.8
    permissions:
      contents: write # for creating branch for pr
    secrets:
      PR_TOKEN_APP_PRIVATE_KEY: ${{ secrets.PR_TOKEN_APP_PRIVATE_KEY }}
    with:
      free-device-space: true

  msrv:
    needs: tidy
    env:
      # Exclude codegen feature because syn's MSRV.
      # NB: Sync with features table in Cargo.toml
      FEATURES: std,cli,critical-section-std,doc,git,sys
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: taiki-e/checkout-action@7d1e50e93dc4fb3bba58f85018fadf77898aee8b # v1.4.2
      - uses: taiki-e/github-actions/free-device-space@dc24bff2366e8ddeea5e0e4168df3d6f4b93f596 # 2026.8.8
      - uses: taiki-e/install-action@07b4745e0c39a41822af610387492e3e53aa222b # v2.83.4
        with:
          tool: cargo-hack
          fallback: none
      - run: cargo hack build -p test-helper --no-dev-deps --features "${FEATURES}" --version-range .. --version-step 2
        if: github.event_name == 'pull_request'
      - run: cargo hack build -p test-helper --no-dev-deps --features "${FEATURES}" --version-range ..
        if: github.event_name != 'pull_request'
